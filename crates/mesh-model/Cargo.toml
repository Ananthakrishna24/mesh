[package]
name = "mesh-model"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Provider adapters, manifests, partial downloads, and cache"

[dependencies]
mesh-core = { workspace = true }
